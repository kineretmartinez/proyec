<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Dashboard Bienestar Social</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #9f2241;
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .login-container {
            background-color: white;
            color: black;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.2);
            width: 100%;
            max-width: 400px;
        }
        .logo {
            max-height: 80px;
            margin-bottom: 1rem;
        }
        .form-control {
            background-color: #f1f1f1;
            border: none;
        }
    </style>
</head>
<body>

<div class="login-container text-center">
    <img src="https://campeche.gob.mx/wp-content/uploads/2023/04/logoh.png" alt="Gobierno de Campeche" class="logo">
    <h2 class="mb-4">Iniciar Sesión</h2>
    
    <form action="/login" method="post">
        <div class="mb-3 text-start">
            <label for="email" class="form-label">Correo electrónico</label>
            <input type="email" class="form-control" id="email" name="email" required>
        </div>
        <div class="mb-3 text-start">
            <label for="password" class="form-label">Contraseña</label>
            <input type="password" class="form-control" id="password" name="password" required>
        </div>

        <% if (error) { %>
            <div class="alert alert-danger mt-3" role="alert">
                <%= error %>
            </div>
        <% } %>

        <button type="submit" class="btn btn-dark w-100 mt-3">Ingresar</button>
    </form>

    <p class="mt-4">
        ¿No tienes una cuenta? <a href="/register" class="text-decoration-none">Regístrate aquí</a>
    </p>
</div>

</body>
</html>